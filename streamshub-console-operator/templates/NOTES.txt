StreamsHub Console Operator has been deployed!

Operator namespace : {{ .Release.Namespace }}
Watch scope        : cluster-wide (all namespaces)
Image              : {{ include "streamshub-console-operator.image" . }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Next steps
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Verify the operator pod is running:

     kubectl get pods -n {{ .Release.Namespace }} \
       -l app.kubernetes.io/name=streamshub-console-operator

2. Deploy a Console instance.

{{- if .Values.deployConsoleInstance }}

   A Console instance was deployed alongside the operator.
   Check its status with:

     kubectl get console -n {{ .Release.Namespace }}
     kubectl describe console -n {{ .Release.Namespace }}

{{- else }}

   No Console instance was deployed. To deploy one together with
   the operator, re-install with:

     helm upgrade --install <release> <chart> \
       --set deployConsoleInstance=true \
       --set consoleInstance.hostname=<your-hostname>

   Or apply a Console CR manually:

     apiVersion: console.streamshub.github.com/v1alpha1
     kind: Console
     metadata:
       name: my-console
       namespace: {{ .Release.Namespace }}
     spec:
       hostname: console.example.com

{{- end }}

3. Check the operator logs if something looks off:

     kubectl logs -n {{ .Release.Namespace }} \
       deployment/{{ include "streamshub-console-operator.fullname" . }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Notes
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
{{- if .Values.openshift.enabled }}
  OpenShift mode is enabled. The cluster-monitoring-view
  ClusterRoleBinding has been created.
{{- else }}
  OpenShift mode is disabled. If running on OpenShift and you
  want Prometheus metrics integration, re-install with:

    --set openshift.enabled=true
{{- end }}

{{- if .Values.serviceMonitor.enabled }}
  A ServiceMonitor has been created. Ensure prometheus-operator
  is installed in your cluster.
{{- else }}
  ServiceMonitor is disabled. To enable Prometheus scraping
  (requires prometheus-operator), re-install with:

    --set serviceMonitor.enabled=true
{{- end }}